<p id="notice"><%= notice %></p>

<h1>Eventos</h1>

<p>
  Automatically deployed on Heroku via push to GitHub.
</p>

<table>
  <% @events.each do |event| %>
    <tr>
      <td><%= event.date %></td>
      <td><%= link_to event.title, event %></td>
    </tr>
  <% end %>
</table>
