%h1 Reggae CDMX

%table.table.table-blank
  %tbody
    - @events.each do |event|
      %tr
        %td= event.date
        %td= link_to event.title, event
        %td= link_to t(:edit), edit_event_path(event)
        %td= link_to t(:destroy), event, method: :delete, data: { confirm: t(:sure?) }
